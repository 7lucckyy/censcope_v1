interface SupportType {
    title: string;
    bgColor: string;
    buttonText: string;
    description: string;
    titleHighlight: string;
}

interface DiscoverType {
    title: string;
    content: string;
}